generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("PRISMA_DB_URL")
}

model user {
  id    String @id @default(uuid()) @map("_id")
  email String @unique
}
